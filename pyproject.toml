[project]
name = "unit-convertor-mcp"
version = "0.1.0"
description = "A minimal MCP server for metric unit conversion."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastmcp>=3.2.4",
]
